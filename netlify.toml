[build]
  functions = "netlify/functions"
  publish = "."

[functions."analyze"]
  timeout = 26

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
